import 'package:flutter/material.dart';

class MyPreferences extends StatelessWidget {
  const MyPreferences({super.key});

  @override
  Widget build(BuildContext context) {
    return const Scaffold(
      body: Center(child: Text('MyPreferences')),
    );
  }
}
