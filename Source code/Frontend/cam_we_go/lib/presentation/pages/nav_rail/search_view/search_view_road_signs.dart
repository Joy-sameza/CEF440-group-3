import 'package:flutter/material.dart';

class SearchViewRoadSigns extends StatelessWidget {
  const SearchViewRoadSigns({super.key});

  @override
  Widget build(BuildContext context) {
    return const Scaffold(
      body: Center(child: Text('SearchViewRoadSigns', style: TextStyle(fontSize: 20))),
    );
  }
}
