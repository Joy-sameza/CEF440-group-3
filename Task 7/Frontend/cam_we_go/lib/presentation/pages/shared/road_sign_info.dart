import 'package:flutter/material.dart';

class RoadSignInfo extends StatelessWidget {
  const RoadSignInfo({super.key});

  @override
  Widget build(BuildContext context) {
    return const Scaffold(
      body: Center(child: Text('RoadSignInfo')),
    );
  }
}
