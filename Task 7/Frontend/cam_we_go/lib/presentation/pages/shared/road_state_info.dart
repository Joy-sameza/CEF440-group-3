import 'package:flutter/material.dart';

class RoadStateInfo extends StatelessWidget {
  const RoadStateInfo({super.key});

  @override
  Widget build(BuildContext context) {
    return const Scaffold(
      body: Center(child: Text('RoadStateInfo')),
    );
  }
}
