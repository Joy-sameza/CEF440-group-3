import 'package:flutter/material.dart';

class PostCrashCare extends StatelessWidget {
  const PostCrashCare({super.key});

  @override
  Widget build(BuildContext context) {
    return const Scaffold(
      body: Center(child: Text('PostCrashCare')),
    );
  }
}
